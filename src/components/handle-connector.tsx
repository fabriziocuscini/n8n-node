import { PlusIcon } from '@phosphor-icons/react';

export const HandleConnector = () => {
  return (
    <div className="handleConnector" role="button" tabIndex={0}>
      <PlusIcon size={12} weight="bold" className="handleConnector__icon" />
    </div>
  );
};
